export const metadata = {
  title: "Create Your Account | Sign Up Now",
  description: "Join us today and unlock exclusive features by signing up. It's quick, easy, and free!",
};

export default function SignUpLayout({ children }) {
  return (
     <main>{children}</main>
  );
}
